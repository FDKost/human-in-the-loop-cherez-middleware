# Environment variables for the Human-in-the-Loop middleware
# Set the Slack webhook URL or token if you want to send review notifications
SLACK_WEBHOOK_URL=
# Timeout in seconds for waiting for a manual review
REVIEW_TIMEOUT=30
# Fallback action if timeout occurs: 'approve' or 'reject'
REVIEW_FALLBACK=reject
